services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=change-me-use-openssl-rand-hex-32
      - DATABASE_URL=sqlite:///./finance.db
    volumes:
      - db-data:/app
    restart: unless-stopped

volumes:
  db-data:
